import pandas as pd
import shap
from sklearn.ensemble import RandomForestRegressor from sklearn.model selection import train test split
from sklearn.datasets import load diabetes
#1. Load and Prep Data
data load diabetes() X pd. DataFrame (data.data, columns data.feature names)
y data.target
X train, X test, y train, y test train teak splitx, y, test size 0.2, random state=42)
#2. Train a Black-Box Model
model RandomForestRegressor (random state=42)
model.fit(X train, y train)
#3. Generate SHAP Values (Explainability Tool) explainer shap. TreeExplainer (model)
shap values explainer.shap values (X test)
